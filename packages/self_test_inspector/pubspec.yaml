name: self_test_inspector
description: Standalone Self-Test Inspector - A web-based interface for testing Flutter apps
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  vm_service: ^14.2.1
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true