name: self_test_bridge
description: WebSocket bridge for self_test MCP server integration
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  self_test:
    path: ../..
  web_socket_channel: ^2.4.0
  go_router: ^14.0.0
  shared_preferences: ^2.2.2
  dio: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
