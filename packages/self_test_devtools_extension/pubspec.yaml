name: self_test_devtools_extension
description: DevTools extension for Self-Test package
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  devtools_extensions: ^0.1.0
  vm_service: ^14.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true