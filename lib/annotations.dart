/// Annotation for marking a button as testable.
class SelfTestButton {
  final String id;
  const SelfTestButton(this.id);
}

/// Annotation for marking an input field as testable.
class SelfTestInput {
  final String id;
  const SelfTestInput(this.id);
}
